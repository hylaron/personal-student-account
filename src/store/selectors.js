export const getShedule = (state) => state.shedule;

export const getGrade = (state) => state.grade;

export const getOrders = (state) => state.orders;

export const getPayPage = (state) => state.payPage;

export const getCard = (state) => state.card;